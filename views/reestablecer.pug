<!DOCTYPE html>
html(lang="en")
  head
    include partials/header
  body.login
    .contenedor-formulario
      h1= nombrePagina
      p Escreva seu email para nova senha 
      if locals.mensajes
        .alertas
          each categoria in Object.keys(locals.mensajes)
            each error in mensajes[categoria]
              .alerta(class=`${categoria}`)
                p= error

      form.caja-login(method="POST" action="/reestablecer")
        .campo
          label(for="email") Email:          
          input(type="text" name="email" placeholder="Seu email" value= email )

        .campo.enviar
          input.boton(type="submit" value="Nova Senha")
        .campo.acciones  
          a(href="/iniciar-sesion") Lembrei! Voltar ao login!


